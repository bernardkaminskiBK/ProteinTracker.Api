# Copy this file to .env and replace the placeholders with local Docker-only credentials.
# Never commit the resulting .env file.
POSTGRES_DB=protein_tracker
POSTGRES_USER=YOUR_POSTGRES_USERNAME
POSTGRES_PASSWORD=YOUR_POSTGRES_PASSWORD
JWT_SIGNING_KEY=REPLACE_WITH_A_RANDOM_SECRET_AT_LEAST_32_CHARACTERS_LONG
